# The source of truth for the release. LibMobileCoin.podspec and the Makefile
# read this file, and Package.swift carries the same two values as literals.
#
# VERSION is the pod version and, with a "v" prefix, the git tag.
# XCFRAMEWORK_SHA256 is the SwiftPM checksum of the release asset for VERSION.
# The release workflow writes both, because the checksum is only known after the
# xcframework is built.
VERSION=6.1.0
XCFRAMEWORK_SHA256=478c932827a68f3b421a6d5c36965c17c6541c0f0856e4c718a26ed470e2a321
